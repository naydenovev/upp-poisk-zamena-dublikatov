## 1. Подготовка

- [x] 1.1 Убедиться, что `src_epf/` (распакованный `python -m v8unpack -E`) соответствует текущему `ПоискИЗаменаДублирующихсяЭлементов.epf`
- [x] 1.2 Зафиксировать точные имена/сигнатуры используемых типовых объектов — подтверждено по эталонной выгрузке конфигурации (`nik_xml`, та же редакция УПП). Фактический вызов на демо-базе не выполнялся (см. п. 4) — открытие формы в Enterprise прошло без ошибок, но кнопка не нажималась.

## 2. Реализация в модуле формы «Форма»

- [x] 2.1 Добавить в `Form/Форма/Form.obj.bsl` новую процедуру `ПометитьДублиНаУдалениеИУдалить(МассивСсылок)`: пометка на удаление (`ПометкаУдаления = Истина`, `Записать()`) каждого элемента, затем вызов `Обработки.УдалениеПомеченныхОбъектов.УдалитьПомеченныеОбъекты(МассивСсылок, ТипыМассив)`
- [x] 2.2 Добавить мягкую проверку наличия `Метаданные.Обработки.Найти("УдалениеПомеченныхОбъектов")` перед вызовом — при отсутствии показать пользователю понятное сообщение вместо падения
- [x] 2.3 Сформировать структуру отчёта (помечено / удалено / не удалено + блокирующие ссылки из `Найденные`) и вывести пользователю (`ТекстовыйДокумент.Показать` при наличии неудалённых, `Предупреждение` при полном успехе)
- [x] 2.4 Добавить диалог подтверждения (`Вопрос`) с указанием количества элементов-кандидатов перед пометкой и удалением
- [x] 2.5 Заменить в `КоманднаяПанельНайденныеСсылкиУдалитьЭлементы` текущий блок `Объект.Удалить()` на вызов новой процедуры; сохранить существующую предварительную проверку `НайденныеСсылки.Количество()=0`
- [x] 2.6 Обновить очистку `НайденныеОбъекты`/`мРезультатыПоиска` — удалять из результатов поиска только реально удалённые элементы (не удалённые остаются в списке как помеченные)
- [x] 2.7 Убедиться, что процедура `АвтозаменаЭлементов` (пакетный путь «Автозамена») осталась без изменений

## 3. Сборка и смоук-тест

- [x] 3.1 Собрать новый EPF: `python -m v8unpack -B src_epf ПоискИЗаменаДублирующихсяЭлементов.new.epf`
- [x] 3.2 Контрольная распаковка нового EPF и сверка BSL-диффом — round-trip идентичен (`diff -rq` пустой)
- [x] 3.3 Открыть новый EPF в толстом клиенте на демо-базе — первая попытка (`/Execute`) дала ложноположительный результат (форма/модуль фактически не компилировались); реальная компиляция подтверждена проверкой модуля пользователем в Конфигураторе, которая нашла и позволила исправить использование `СтрШаблон`/`СтрСоединить`, недоступных в режиме совместимости этой обработки

## 4. Функциональная проверка на демо-базе

- [x] 4.1 Подготовить/найти в демо-базе тестовые дубли в справочнике для проверки — выполнено пользователем
- [x] 4.2 Выполнить поиск и замену для группы дублей, нажать «Удалить элементы», подтвердить — выполнено пользователем
- [x] 4.3 Проверить отчёт и фактическое состояние объектов — подтверждено пользователем ("все работает")
- [x] 4.4 Проверить сценарий отмены в диалоге подтверждения — подтверждено пользователем
- [x] 4.5 Проверить, что «Автозамена» продолжает работать как раньше — подтверждено пользователем

## 5. Публикация

- [x] 5.1 Написать README (описание доработки, что выполнено при помощи Claude Code)
- [x] 5.2 Закоммитить изменения в локальном репозитории `e:\project\obr_double&delete\`
- [ ] 5.3 После подтверждения пользователем — создать отдельный репозиторий на GitHub (`naydenovev`) и запушить
